# ============================================================================
# ARQUIVO DE EXEMPLO: .env.example
# ============================================================================
# 
# INSTRUÇÕES DE USO:
# 1. Copie este arquivo e renomeie para ".env"
# 2. Preencha com suas credenciais e configurações reais
# 3. NUNCA faça commit do arquivo .env (sempre adicione em .gitignore)
# 4. Mantenha este arquivo atualizado com novos parâmetros
#
# ============================================================================

# --- CONFIGURAÇÕES DO SISTEMA ALVO (NeoERP) ---

# URL base do sistema
SYS_URL="https://neo.solucoes.plus/"

# Credenciais de login
SYS_USERNAME="seu.email@empresa.com"
SYS_PASSWORD="sua_senha_aqui"

# OTP (One-Time Password) - Chave para 2FA
# Gere uma nova chave no sistema de autenticação se necessário
SYS_SECRET_OTP="XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX="

# URL do serviço de geração de OTP (deixe como está se usar padrão local)
OTP_URL="http://192.168.11.86:8001/generate_otp"


# --- CONFIGURAÇÕES DO NAVEGADOR ---

# Navegador a ser utilizado: firefox, chrome ou edge
BROWSER=chrome

# Executar em modo headless (sem interface gráfica)
# true = modo headless (recomendado para produção)
# false = com interface (útil para debug)
HEADLESS=true

# Número de tentativas para download de arquivos
RETRIES_DOWNLOAD=3

# Timeout (em segundos) para cada tentativa de download
TIMEOUT_DOWNLOAD=60

# ✅ NOVO: Delay entre tentativas de download (em segundos)
# Recomendado: 120-180 segundos para arquivos grandes ou servidor lento
# Quanto maior, mais tempo entre tentativas (permite servidor recuperar)
DOWNLOAD_RETRY_DELAY=120


# --- CONFIGURAÇÕES DE DIRETÓRIOS ---

# Diretório final onde arquivos são salvos (em produção, geralmente unidade de rede)
DESTINO_FINAL_DIR="Y:"

# Diretório de downloads do robô (opcional)
# Se não configurado, usa "./downloads" na raiz do projeto
# Exemplos:
#   DOWNLOADS_DIR=./downloads          (pasta local relativa)
#   DOWNLOADS_DIR=/tmp/downloads       (path absoluto Linux)
#   DOWNLOADS_DIR=C:\temp\downloads    (path absoluto Windows)
# DOWNLOADS_DIR=./downloads


# --- CONFIGURAÇÕES DE BANCO DE DADOS SQL SERVER ---

# Servidor SQL Server (endereço + porta)
# Formato: "IP,PORTA" ou "HOSTNAME,PORTA" ou "IP\INSTANCIA"
DB_SERVER="192.168.11.200,1434"

# Nome do banco de dados
DB_DATABASE="rpa_neocrm"

# Usuário do SQL Server (autenticação SQL, não Windows)
DB_USERNAME="dbAdmin"

# Senha do usuário SQL
DB_PASSWORD="sua_senha_sql_aqui"

# Driver ODBC para conexão
# Opções comuns:
#   "ODBC Driver 18 for SQL Server"  (mais recente, recomendado)
#   "ODBC Driver 17 for SQL Server"  (compatível com SQL Server 2016+)
#   "ODBC Driver 11 for SQL Server"  (legado)
DB_DRIVER="ODBC Driver 17 for SQL Server"


# --- CONFIGURAÇÕES DE TESTES E DEBUG ---

# Modo de execução seca (dry-run)
# true = apenas loga o que seria enviado, sem inserir no banco
# false = executa normalmente e insere dados no banco
DRY_RUN=false

# Tamanho de batch para envio de registros
# Quanto maior, menos requisições (mais rápido)
# Quanto menor, mais requisições (mais seguro, menos memória)
# Recomendado: 25-50
BATCH_SIZE=25

# Número de tentativas para envio de cada batch
# Se um batch falhar, tenta novamente essa quantidade de vezes
POST_RETRIES=3

# Base exponencial para backoff entre tentativas
# Tempo de espera = BACKOFF_BASE ^ tentativa (em segundos)
# Exemplo com 1.5:
#   1ª tentativa: falha
#   2ª tentativa: aguarda 1.5^1 = 1.5 segundos
#   3ª tentativa: aguarda 1.5^2 = 2.25 segundos
# Recomendado: 1.5 ou 2.0
BACKOFF_BASE=1.5


# --- CONFIGURAÇÕES OPCIONAIS ---

# Adicione aqui outras configurações conforme necessário
# Exemplos:
# NOTIFICACAO_EMAIL="admin@empresa.com"
# AMBIENTE="producao"
# LOG_LEVEL="DEBUG"
